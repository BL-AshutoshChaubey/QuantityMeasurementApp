server:
  port: ${PORT:8080}

eureka:
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:eureka-server}
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://quantitymeasurementapp.railway.internal:${server.port}/eureka/
